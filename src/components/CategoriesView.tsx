import React, {useMemo,useState} from 'react'
export default function CategoriesView({matches,teamsById}:{matches:any[], teamsById:any}){ const cats=['Alevín Femenino','Alevín Masculino','Infantil Femenino','Infantil Masculino','Cadete Femenino','Cadete Masculino','Juvenil Femenino','Juvenil Masculino']; const [open,setOpen]=useState<string|null>(null); const base = matches; return (<div className='grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4'>{cats.map(c=> { const list = base.filter(m=> m.category===c); return (<div key={c} className='p-3 border rounded bg-white'><div className='flex justify-between items-center'><div className='font-semibold'>{c}</div><div className='text-sm text-slate-500'>{list.length} partidos</div></div><div className='mt-2'><button onClick={()=>setOpen(open===c? null : c)} className='px-3 py-2 bg-slate-100 rounded'>Ver</button></div>{open===c && (<div className='mt-3'>{list.map(m=> <div key={m.id} className='p-2 border rounded mb-2'><div className='text-sm'>{new Date(m.datetime).toLocaleString()}</div><div className='mt-1'>{teamsById[m.a]?.name} vs {teamsById[m.b]?.name}</div></div>)}</div>)}</div>) })}</div>) }
