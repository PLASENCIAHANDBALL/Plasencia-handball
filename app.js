// small JS to show dynamic behaviors (pure JS, no frameworks)
document.addEventListener('DOMContentLoaded', function(){
  // placeholder: could fetch live data
  console.log("Plasencia Handball static site loaded");
});